export interface UserType {
  user: {
    id: string;
    firstName: string;
    lastName: string;
    country: string;
    adresse: string;
    postalCode: string;
    email: string;
    password: string;
  };
}
